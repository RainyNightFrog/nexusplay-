---
description: 禁止自動部署正式站；僅在用戶明確要求時才 vercel --prod
alwaysApply: true
---

# 勿自動部署正式站

修改程式後**預設不要**執行正式站部署（`vercel --prod`、`vercel deploy --prod` 等）。

## 預設流程

1. 在本地改完並驗證（`npm run dev` / localhost）
2. 向用戶說明改了什麼
3. **停在這裡**，等用戶決定要不要上線

## 何時才可部署正式站

僅當用戶**明確**用以下意圖下指令時才部署，例如：

- 「部署」「上線」「更新正式站」「推到 production」「vercel --prod」

以下**不算**授權部署：

- 「修好了嗎」「確認一下」「改這個」「看看效果」
- 用戶只開正式站網址回報問題（那是排查對象，不是部署指令）

## 範例

**❌ 不好：** 修完 UI bug 後自動 `npx vercel --prod`

**✅ 正確：** 修完後說明已改好，請用戶在本地或現有正式站查看；若要上線再跟你說
