---
description: 每次完成程式修改後必須 commit 並 push 到 GitHub
alwaysApply: true
---

# 每次修改必須同步 Commit 與 Push

完成任何**實作、修復、重構、設定調整**且工作區有變更時，**必須**在本輪回覆使用者**之前**自動 `commit` 並 `push` 到遠端，無需再問「要不要 commit / push」。

## 何時執行

- 已修改、新增或刪除專案檔案（含 `.cursor/rules/`、設定、腳本、文件若為任務一部分）
- 一輪對話內的實作或修復已結束，即使只改一兩個檔案也要提交
- 部署前修復（例如 `vercel.json`、build 錯誤）同樣必須提交，避免正式站與 GitHub 脫節

## 何時不執行

- 僅回答問題、code review、規劃，**沒有**改檔
- 使用者明確說「先不要 push」「不要提交」「只改本地」
- 沒有任何可提交的變更
- 變更含 `.env.local`、金鑰、token 等敏感檔（勿提交；改提交 `.env.example` 等安全範本）

## 流程（每次提交前）

並行執行：

1. `git status`
2. `git diff`（含 staged / unstaged）
3. `git log -5 --oneline`（沿用既有 commit 風格）

然後依序：

1. `git add` 相關檔案（排除敏感檔）
2. 撰寫 1–2 句、說明 **why** 的 commit message（繁中或英文皆可，與近期 log 一致）
3. `git commit`
4. `git push -u origin HEAD`（若尚未設定 upstream）或 `git push`
5. `git status` 確認成功

## 安全限制

- **禁止** `git push --force` 到 `main` / `master`
- **禁止** `git config` 變更
- **禁止** `--no-verify`、hard reset 等破壞性指令（除非使用者明確要求）
- 若 push 失敗（權限、衝突、CI），在回覆中說明原因與建議下一步，不要略過不提

## 回覆使用者

推送成功後，簡短附上：

- branch 名稱
- commit message
- 若有部署相關變更，提醒 Vercel 是否會自動部署或需手動部署
